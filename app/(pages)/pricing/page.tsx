import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "SEO Title",
  description: "SEO description",
  keywords: ["Pricing", "text", "next"],
};

const Pricing = () => {
  return (
    <div>
      Pricing
    </div>
  )
}

export default Pricing
