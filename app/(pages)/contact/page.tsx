import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "SEO Title",
  description: "SEO description",
  keywords: ["Contact", "text", "next"],
};

const ContactPage = () => {
  return <div>ContactPage</div>;
};

export default ContactPage;
